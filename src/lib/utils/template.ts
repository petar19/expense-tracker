// Kept in sync with expense-tracker-bot/src/template.ts (the WhatsApp bot),
// which is what actually renders this — this copy exists so the admin panel
// can show the default as a placeholder without needing to ask the bot.
export const DEFAULT_ANNOUNCEMENT_TEMPLATE = '{sender}: *{name}, {price}€*';
